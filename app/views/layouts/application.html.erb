<!DOCTYPE html>
<html>
<head>
  <title>Cobot Slack</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <% [:notice, :success, :failure].each do |flash_type| %>
    <% if flash[flash_type] %>
      <div id="flash" class="cb-top-notice <%= flash_type %>">
        <div class="cb-container">
          <p><%== flash[flash_type] %></p>
        </div>
      </div>
    <% end %>
  <% end %>

  <div class="cb-section cb-section--top-bar">
    <div class="cb-container">
      <div class="nav__logo">
        <%= link_to image_tag('cobot_assets/cobot-logo.svg'), root_url, class: 'cb-logo' %>
        <%= link_to image_tag('slack.svg'), root_url, class: 'cb-integration-logo' %>
      </div>
    </div>
  </div>

  <div class="cb-site-main">
    <%= yield %>
  </div>
</body>
</html>
